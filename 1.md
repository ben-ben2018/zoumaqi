这是一份专为**网页版**（HTML5 / JavaScript）复刻《燕云十六声》「打马棋」玩法而设计的工业级 Prompt。全文严格遵循中文要求，融合了最佳实践，并基于游戏攻略进行了结构化和规范化，可供现代 AI 编程工具（如 Cursor、Copilot、Claude 3.7/4）直接作为系统指令输入。

---

## 一、 目标与技术栈

使用 **HTML5 / JavaScript** 技术栈完整复刻《燕云十六声》中的「打马棋」玩法核心。

- **核心框架**：**React 18+**（TypeScript 可选）构建 UI，**boardgame.io 0.50+** 作为游戏状态与回合制引擎，天然支持多人联机和状态同步。
- **棋盘渲染**：Canvas 或 Phaser 3，实现棋盘绘制、棋子移动动画与骰子动画。
- **样式方案**：TailwindCSS + CSS Modules。
- **状态管理**：boardgame.io 内置状态机 + React Context。
- **网络支持**：boardgame.io 内置 WebSocket 服务端（支持本地多人/AI/远程联机）。

---

## 二、 核心游戏规则

1. **基础玩法**：2v2 回合制棋盘竞速，棋盘共 50 格（环形赛道）。任意一队有**一名玩家**率先抵达终点（第 50 格）即该队获胜。
2. **回合流程**：掷骰子 → 应用被动/卡牌/Buff 修正点数 → 移动 → 触发格子事件 → 卡牌购买/使用阶段 → 技能使用阶段 → 切换玩家。
3. **经济系统**：棋珍（游戏货币），用于商店购买卡牌。基础收入：每回合固定收入 + 移动收入 + 卡牌/技能额外收入。
4. **卡牌管理**：基础手牌上限 6 张，墨山道为 8 张。卡牌可通过商店购买、技能获取、奇遇格获得。

---

## 三、 数据结构定义（TypeScript 版本）

请严格按照以下接口、枚举定义数据模型。

```typescript
// 核心枚举
export enum PlayerTeam { RED = 0, BLUE = 1 }
export enum PlayerStatus { NORMAL = 0, SKIP_TURN = 1, FROZEN = 2 }
export enum CardType { MOVEMENT = 0, BUFF = 1, DEBUFF = 2, ATTACK = 3, ECONOMY = 4 }
export enum SkillTarget { SELF = 0, ALLY = 1, ENEMY = 2 }
export enum MapEventType { SHOP = 0, MYSTERY = 1, BOSS = 2, EMPTY = 3 }

export enum Sect {
  QINGXI = 0,     // 清溪
  LIYUAN = 1,     // 梨园
  TIANQUAN = 2,   // 天泉
  GUYUN = 3,      // 孤云
  SANGENGTIAN = 4, // 三更天
  KUANGLAN = 5,   // 狂澜
  ZUIHUAYIN = 6,  // 醉花阴
  MOSHANDAO = 7,  // 墨山道
  JIULIUMEN = 8   // 九流门
}

// 卡牌数据
export interface CardData {
  id: string;
  name: string;
  description: string;
  type: CardType;
  price: number;           // 棋珍价格
  targetType: SkillTarget;
  isPassive: boolean;
  effect: (G, ctx, ...args) => void; // 效果函数
}

// 玩家数据
export interface PlayerData {
  id: string;
  team: PlayerTeam;
  sect: Sect;
  position: number;        // 0-49
  gold: number;
  handCards: CardData[];
  buffs: Map<string, { remainingTurns: number; value: any }>;
  debuffs: Map<string, { remainingTurns: number; value: any }>;
  status: PlayerStatus;
  activeSkillCooldown: number;
}

// 棋盘格子数据
export interface TileData {
  index: number;
  eventType: MapEventType;
  eventData?: any;          // 商店卡片池、奇遇奖励等
}
```

---

## 四、 详细玩法实现（boardgame.io 配置）

请基于 boardgame.io 的 `Game` 配置对象实现以下逻辑。**重要**：由于原版游戏中部分数据（如卡牌价格表、地图事件精确位置）未有完全公开的精确数据，开发时请预留配置接口，标注 `// TODO` 便于后续填充。

### 4.1 游戏配置 (`gameConfig.ts`)

```typescript
import { Game, Ctx, PlayerID, Move } from 'boardgame.io';
import { PlayerData, Sect, PlayerTeam, CardData, TileData } from './types';

export const DamaqiGame: Game<GameState> = {
  name: 'damaqi',
  
  setup: (ctx, setupData) => {
    // 初始化4名玩家：位置0，金币0（天泉除外+100），手牌为空
    // 按顺序分配门派（1P可选，AI随机）
    // 初始化50格棋盘，商店格（索引3,10,20,30,40）、奇遇格（索引6,13,23,33,43）
  },
  
  moves: {
    // 掷骰子 Move
    rollDice: (G, ctx) => {
      const player = G.players[ctx.currentPlayer];
      if (player.hasRolledThisTurn) return;
      
      let roll = Math.floor(Math.random() * 6) + 1;
      // 应用被动技能：孤云"算无遗策"（距离最近敌人4-6格+3，7-9格+5，>10格+7）
      // 应用被动技能：梨园"名伶登台"（非第一名+5）
      // 应用醉花阴 debuff（-3）
      // 应用 Buff 修正（阴阳迷踪步等）
      // 应用骰子套效果（集齐吉·好兆骰+吉·好运骰后概率100%触发）
      
      player.lastRoll = roll;
      player.hasRolledThisTurn = true;
      G.pendingRoll = roll;
      ctx.events?.endTurn(); // 或进入移动阶段
    },
    
    // 移动玩家
    movePlayer: (G, ctx, steps: number) => {
      const player = G.players[ctx.currentPlayer];
      let newPos = Math.min(player.position + steps, G.board.totalTiles - 1);
      player.position = newPos;
      
      // 触发格子事件（商店/奇遇/BOSS）
      const tile = G.board.tiles[newPos];
      if (tile.eventType === MapEventType.SHOP) {
        G.currentShop = generateShopCards(); // 生成商店卡片池
        G.pendingShop = true;
      } else if (tile.eventType === MapEventType.MYSTERY) {
        applyMysteryEvent(player);
      }
      
      // 胜负判定
      if (newPos >= G.board.totalTiles - 1) {
        ctx.events?.endGame({ winner: player.team });
      }
      
      player.hasMovedThisTurn = true;
    },
    
    // 购买卡牌
    buyCard: (G, ctx, cardId: string) => {
      const player = G.players[ctx.currentPlayer];
      const card = G.shopCards.find(c => c.id === cardId);
      if (player.gold >= card.price && player.handCards.length < (player.sect === Sect.MOSHANDAO ? 8 : 6)) {
        player.gold -= card.price;
        player.handCards.push(card);
        G.shopCards = G.shopCards.filter(c => c.id !== cardId);
      }
    },
    
    // 使用卡牌
    useCard: (G, ctx, cardId: string, targetPlayerId: string) => {
      const player = G.players[ctx.currentPlayer];
      const card = player.handCards.find(c => c.id === cardId);
      if (!card) return;
      
      // 执行卡牌效果（调用 CardExecutor）
      executeCardEffect(card, player, G.players[targetPlayerId], G, ctx);
      
      // 移除卡牌
      player.handCards = player.handCards.filter(c => c.id !== cardId);
    },
    
    // 使用门派主动技能
    useActiveSkill: (G, ctx, targetPlayerId?: string) => {
      const player = G.players[ctx.currentPlayer];
      if (player.activeSkillCooldown > 0) return;
      
      switch (player.sect) {
        case Sect.QINGXI:   // 坐看云起 → 获得妙手回春
          player.handCards.push(MIAOSHOU_HUICHI_CARD);
          break;
        case Sect.LIYUAN:   // 请君打榜 → 偷取卡牌或15棋珍
          const target = G.players[targetPlayerId];
          if (target.handCards.length > 0) {
            const stolen = target.handCards.pop();
            player.handCards.push(stolen);
          } else {
            const stolenGold = Math.min(target.gold, 15);
            target.gold -= stolenGold;
            player.gold += stolenGold;
          }
          break;
        // ... 其他门派实现
      }
      
      player.activeSkillCooldown = 2; // CD 2回合
    }
  },
  
  turn: {
    minMoves: 1,
    maxMoves: 1,
    stages: {
      roll: { moves: ['rollDice'], next: 'move' },
      move: { moves: ['movePlayer'], next: 'shop' },
      shop: { moves: ['buyCard'], next: 'card' },
      card: { moves: ['useCard'], next: 'skill' },
      skill: { moves: ['useActiveSkill'], next: 'end' }
    }
  },
  
  endIf: (G, ctx) => {
    // 检查是否有队伍到达终点
    for (const player of Object.values(G.players)) {
      if (player.position >= G.board.totalTiles - 1) {
        return { winner: player.team };
      }
    }
  }
};
```

### 4.2 卡牌效果系统（CardExecutor）

```typescript
// 核心卡牌效果实现
export const CardEffects = {
  // 凌云踏：立刻向前移动3-6格
  lingyun_ta: (caster, G, ctx) => {
    const steps = Math.floor(Math.random() * 4) + 3; // 3-6
    G.pendingMovement = steps;
  },
  
  // 阴阳迷踪步：友方移动点数+5，持续2回合
  yinyang_mizongbu: (caster, target, G) => {
    target.buffs.set('yinyang', { remainingTurns: 2, value: 5 });
  },
  
  // 无相金身：免疫奇袭3回合
  wuxiang_jinshen: (caster, G) => {
    caster.buffs.set('jinshen', { remainingTurns: 3, value: true });
  },
  
  // 散财消灾：失去20棋珍，免疫奇袭1回合
  sancai_xiaozai: (caster, G) => {
    caster.gold = Math.max(caster.gold - 20, 0);
    caster.buffs.set('jinshen', { remainingTurns: 1, value: true });
  },
  
  // 摄星拿月：偷取敌方一张卡牌
  shexing_nayue: (caster, target, G) => {
    if (target.handCards.length > 0) {
      const stolen = target.handCards.pop();
      caster.handCards.push(stolen);
    }
  },
  
  // 叨叨不叨叨：使目标失去一张随机卡牌
  daodao_budaodao: (caster, target, G) => {
    if (target.handCards.length > 0) {
      const lostIndex = Math.floor(Math.random() * target.handCards.length);
      target.handCards.splice(lostIndex, 1);
    }
  },
  
  // 吉·好兆骰 + 吉·好运骰：套装效果（集齐后概率100%触发）
  jixiang_set: (player, roll, G) => {
    const hasHaozao = player.handCards.some(c => c.id === 'jixiang_haozao');
    const hasHaoyun = player.handCards.some(c => c.id === 'jixiang_haoyun');
    if (hasHaozao && hasHaoyun) {
      // 点数1-5时 +4 并得1牌，≥6时 +6 并得2牌
      if (roll <= 5) {
        return { addedSteps: 4, extraCards: 1 };
      } else {
        return { addedSteps: 6, extraCards: 2 };
      }
    }
    return null;
  },
  
  // 一掷千金：消耗全部棋珍，移动(棋珍/5)步数，至多20格
  yizhi_qianjin: (caster, G) => {
    const steps = Math.min(Math.floor(caster.gold / 5), 20);
    caster.gold = 0;
    G.pendingMovement = steps;
  },
  
  // 生财有道：每次投掷获得(移动格数*1)棋珍，持续2回合
  shengcai_youdao: (caster, G) => {
    caster.buffs.set('shengcai', { remainingTurns: 2, value: null });
  }
};

// 执行卡牌效果的主函数
export function executeCardEffect(card: CardData, caster: PlayerData, target: PlayerData | null, G: GameState, ctx: Ctx) {
  const effect = CardEffects[card.id];
  if (effect) {
    effect(caster, target, G, ctx);
  }
}
```

### 4.3 9大门派技能实现

| 门派 | 主动技能 | 主动技能实现 | 被动技能 | 被动技能实现 |
| :--- | :--- | :--- | :--- | :--- |
| **清溪** | 坐看云起 | 获得【妙手回春】卡牌（清除debuff，下两次投掷+4/+6） | 一命一价 | 队友被奇袭时，支付10棋珍或一张卡牌解除其"无法行动" |
| **梨园** | 请君打榜 | 从任意敌方玩家处偷取一张卡牌或15棋珍 | 名伶登台 | 非第一名时，下一次投掷+5 |
| **天泉** | 千金取义 | 消耗15棋珍，获得任意两张卡牌 | 仗义疏财 | 开局+100棋珍，其他玩家收到40棋珍 |
| **孤云** | 长驱直入 | 锁定距终点最近的敌方，获得【凌虚一指】并移动最多6格；若未发起奇袭，下次-3 | 算无遗策 | 距最近敌人4-6格:+3，7-9格:+5，>10格:+7 |
| **三更天** | 天怒刀法 | 对-6至6格距离的玩家发起奇袭，使其跳过一回合 | 杀生夺业 | 击杀后，获得任意两张卡牌，向前移动6格 |
| **狂澜** | 千里奔袭 | 下两次投掷可额外移动（-2-6格） | 与子同袍 | 若队友落后自己>4格，队友下一次+3 |
| **醉花阴** | 花醉三千 | 给敌方上debuff，下两轮-3，每回合结束向自己支付15棋珍 | 百花齐放 | 场上醉花阴玩家越多，投掷点数越高（每个+2） |
| **墨山道** | 兼爱非攻 | 自己和队友各获得一张随机卡牌 | 匠作圣手 | 手牌上限+2（8张） |
| **九流门** | 偷天换日 | 概率性改变敌方/自己的骰子点数 | 举起鼠爪 | 他人移动终点与自己同位置时，自动乞讨30棋珍或两张卡牌 |

---

## 五、 UI组件架构（React）

```tsx
// App.tsx - 主入口
import { Client } from 'boardgame.io/react';
import { DamaqiGame } from './game/gameConfig';
import { DamaqiBoard } from './components/DamaqiBoard';

const DamaqiClient = Client({ game: DamaqiGame, board: DamaqiBoard });

function App() {
  return <DamaqiClient />;
}

// DamaqiBoard.tsx - 游戏界面
export const DamaqiBoard: React.FC<BoardProps> = ({ G, ctx, moves, playerID }) => {
  const currentPlayer = G.players[ctx.currentPlayer];
  const myPlayer = G.players[playerID];
  
  return (
    <div className="board-container" style={{ background: 'url(/textures/paper-bg.jpg)' }}>
      {/* 顶部：回合指示器 + 队伍比分 */}
      <TurnIndicator currentPlayer={ctx.currentPlayer} turnOrder={ctx.playOrder} />
      
      {/* 棋盘区域（Canvas + Phaser）*/}
      <BoardCanvas tiles={G.board.tiles} players={Object.values(G.players)} />
      
      {/* 玩家状态面板（四个角）*/}
      <PlayerPanel player={myPlayer} isCurrent={ctx.currentPlayer === playerID} />
      
      {/* 骰子区域 */}
      <DicePanel 
        onRoll={() => moves.rollDice()} 
        lastRoll={currentPlayer?.lastRoll}
        disabled={ctx.currentPlayer !== playerID || currentPlayer?.hasRolledThisTurn}
      />
      
      {/* 手牌区域（底部）*/}
      <HandCards 
        cards={myPlayer?.handCards || []} 
        onUseCard={(cardId, targetId) => moves.useCard(cardId, targetId)}
        disabled={ctx.currentPlayer !== playerID}
      />
      
      {/* 商店弹窗 */}
      {G.pendingShop && (
        <ShopModal 
          cards={G.shopCards} 
          playerGold={myPlayer?.gold}
          onBuy={(cardId) => moves.buyCard(cardId)}
          onClose={() => G.pendingShop = false}
        />
      )}
    </div>
  );
};
```

---

## 六、 美术风格与视觉规范

- **整体基调**：纯粹写实国风，水墨与灰青色调，体现五代十国末期的东方武侠质感。
- **棋盘设计**：类似《千里江山图》的青绿山水长卷，卷轴式横向展开，每格为独立微场景（凉亭、断桥、竹林等）。
- **UI风格**：古风绢本质感，仿宣纸米黄底色，深褐色木纹边框，毛笔手写字体。
- **动画效果**：棋子移动使用缓动动画（ease-in-out），骰子使用3D旋转效果，卡牌使用翻转入场效果。
- **音效**：骰子落地声、卡牌使用声、移动脚步声、胜利号角声。

---

## 七、 开发优先级与里程碑

| 阶段 | 目标 | 产出 |
| :--- | :--- | :--- |
| **Phase 1** | 基础回合制、掷骰子、移动、胜负判定 | 4名玩家（本地）可轮流掷骰移动 |
| **Phase 2** | 卡牌系统（商店、购买、手牌） | 凌云踏、无相金身等基础卡牌可用 |
| **Phase 3** | 门派技能（清溪、孤云、三更天） | 主动技能与被动效果完整实现 |
| **Phase 4** | 完整9大门派 + 完整卡牌池 | 所有门派与卡牌可用 |
| **Phase 5** | 棋盘事件 + 动画特效 | 奇遇格、BOSS格完整实现 |
| **Phase 6** | 多人联机（boardgame.io 内置） | 4名真实玩家同时游戏 |

---

## 八、 给 AI 的直接指令

> 你现在是一个专业的 React + boardgame.io 前端游戏开发工程师。请严格按照上述架构设计，分步骤输出以下代码：
> 
> 1. **项目初始化**：`package.json` 配置（React 18 + boardgame.io + Phaser 3 + TailwindCSS），Vite 构建配置。
> 2. **类型定义**：`types/index.ts`，包含所有枚举、接口（PlayerData、CardData、TileData）。
> 3. **游戏核心**：`game/gameConfig.ts`，boardgame.io Game 配置，包含 setup、moves、turn 配置。
> 4. **卡牌系统**：`game/cards/cardData.ts` + `game/cards/cardEffects.ts`，实现以下卡牌：凌云踏、阴阳迷踪步、无相金身、散财消灾、摄星拿月、叨叨不叨叨、凌虚一指、一掷千金、生财有道、聚宝盆、千里目、飒沓流星、吉·好兆骰、吉·好运骰（含套装效果）、妙手回春。价格标注 `// TODO` 便于后续调整。
> 5. **门派技能**：`game/skills/sectSkills.ts`，9大门派的主动/被动技能完整实现。
> 6. **棋盘系统**：`game/board/boardData.ts`，50格棋盘配置（商店格索引3,10,20,30,40，奇遇格索引6,13,23,33,43，BOSS格索引15,25,35,45）。商店卡片池生成逻辑（`generateShopCards`）标注 `// TODO` 便于后续精确配置。
> 7. **UI组件**：`components/` 目录下所有组件（BoardCanvas、DicePanel、HandCards、PlayerPanel、ShopModal、TurnIndicator）。
> 8. **Phaser 集成**：`game/board/BoardScene.ts`，Phaser 场景实现棋盘渲染和棋子移动动画。
> 
> 所有代码需遵循 TypeScript 严格模式，使用函数式组件和 Hooks，并提供详细的中文注释。对于未完全明确的数据（如卡牌价格、地图事件具体内容、商店刷新规则），请预留接口并标注 `// TODO` 便于后续填充。

